name: Test Suite (JavaScript & Rails)

on:
  pull_request:
    branches: [main]
  push:
    branches: [main, feature/*, hotfix/*]

permissions:
  pull-requests: write
  contents: read

jobs:
  javascript-tests:
    name: React / TypeScript Tests
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build JavaScript
        run: npm run build

  rails-tests:
    name: Rails / RSpec Tests
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_HOST_AUTH_METHOD: trust
          POSTGRES_DB: honest_voice_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18.x
          cache: npm

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: Install Node dependencies
        run: npm ci

      - name: Setup database
        env:
          RAILS_ENV: test
          DATABASE_URL: postgresql://postgres@localhost/honest_voice_test
        run: |
          bundle exec rails db:create 2>&1 || echo "Database already exists"
          bundle exec rails db:migrate

      - name: Run Rails tests
        env:
          RAILS_ENV: test
          DATABASE_URL: postgresql://postgres@localhost/honest_voice_test
        run: bundle exec rspec

  test-summary:
    name: Test Summary
    runs-on: ubuntu-latest
    needs: [javascript-tests, rails-tests]
    if: always()

    steps:
      - name: Check test results
        run: |
          echo "✅ JavaScript Tests: ${{ needs.javascript-tests.result }}"
          echo "✅ Rails Tests: ${{ needs.rails-tests.result }}"

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v9
        with:
          script: |
            const jsResult = '${{ needs.javascript-tests.result }}';
            const railsResult = '${{ needs.rails-tests.result }}';

            let summary = '## 🧪 Test Results\n\n';
            summary += jsResult === 'success' ? '✅ **React/TypeScript Tests**: Passed\n' : '❌ **React/TypeScript Tests**: Failed\n';
            summary += railsResult === 'success' ? '✅ **Rails Tests**: Passed\n' : '❌ **Rails Tests**: Failed\n';

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: summary
            });
